/// <reference types="react" />
import { PropsType } from '../';
declare const formatFraction: (value: string, { fractionFormat, superScriptFraction }: PropsType, isRound: boolean) => string | JSX.Element;
export default formatFraction;
