import { FunctionComponent } from 'react';
declare type PropsType = {
    by?: 'margin';
    before?: boolean;
    after?: boolean;
};
declare const Separated: FunctionComponent<PropsType>;
export default Separated;
